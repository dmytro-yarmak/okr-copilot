---
description: Draft a new OKR from scratch in guided mode.
---

Read `skills/okr-write.md` and execute it exactly.

Requirements:
- Ask one question at a time.
- Scan `context/` recursively before drafting.
- Use context evidence in objective/KR recommendations.
- Validate `context/okr-ground-rules.md` for completion (not only existence).
- If ground rules are missing/not configured, propose `/okr-ground-rules` first.
- Keep ownership with the human; guide, do not replace.

Use `$ARGUMENTS` as optional hint (`team`, `department`, or `company`).
