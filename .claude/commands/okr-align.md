---
description: Run a mostly autonomous alignment and coverage analysis across OKR sets.
---

Read `skills/okr-align.md` and execute it exactly.

Requirements:
- Scan `context/` recursively before analyzing.
- Validate `context/okr-ground-rules.md` for completion (not only existence).
- Ask only minimal blocking clarifications, then provide full alignment report.
- If no OKR sets are provided, ask for paste/link or point to files.
- Return executive summary, alignment & coverage tables, and recommendations.

Use `$ARGUMENTS` as optional analysis hint.
