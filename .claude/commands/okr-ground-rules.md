---
description: Define and save reusable OKR ground rules.
---

Read `skills/okr-ground-rules.md` and execute it exactly.

Requirements:
- Ask one question at a time.
- Before questions, scan `context/` recursively and summarize constraints relevant to OKR ground rules.
- Save finalized rules to `context/okr-ground-rules.md` after confirmation.

Use `$ARGUMENTS` as optional scope hint (team, multi-team, or company-wide).
